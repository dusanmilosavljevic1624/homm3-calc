export default class Specialty {
	constructor(slug, name, image, description, type) {
		this.slug = slug;
		this.name = name;
		this.image = image;
		this.description = description;
		this.type = type;
	}
}
